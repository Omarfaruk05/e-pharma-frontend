export enum tagTypes {
  user = "user",
  admin = "admin",
  auth = "auth",
  category = "category",
  variant = "variant",
  product = "product",
  order = "order",
  otp = "otp",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.admin,
  tagTypes.auth,
  tagTypes.category,
  tagTypes.variant,
  tagTypes.product,
  tagTypes.order,
  tagTypes.otp,
];
