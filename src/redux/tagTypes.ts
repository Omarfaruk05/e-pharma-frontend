export enum tagTypes {
  user = "user",
  admin = "admin",
  auth = "auth",
  product = "product",
  order = "order",
  otp = "otp",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.admin,
  tagTypes.auth,
  tagTypes.product,
  tagTypes.order,
  tagTypes.otp,
];
